-- This script lists all records from second_table
-- Displays score and name columns (in that order)
-- Orders results by score (highest first)

SELECT score, name
FROM second_table
ORDER BY score DESC;
