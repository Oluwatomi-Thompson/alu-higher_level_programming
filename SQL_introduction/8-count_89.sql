-- This script counts the number of records with id = 89 in first_table

-- Select the count of records where id equals 89
SELECT COUNT(*)
-- From the first_table in the database
FROM first_table
-- Where the id column has a value of 89
WHERE id = 89;
